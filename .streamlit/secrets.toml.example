# Copy this file to `secrets.toml` in the same folder (`.streamlit/secrets.toml`).
# Do not commit `secrets.toml` if it contains private links.

# Power BI: paste the embed / "Publish to web" / secure embed URL from Power BI Service.
# Example (your URL will differ):
# POWERBI_REPORT_EMBED_URL = "https://app.powerbi.com/view?r=eyJrIjoi..."

POWERBI_REPORT_EMBED_URL = ""
